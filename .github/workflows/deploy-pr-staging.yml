name: Deploy PR Staging Branch

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - closed

concurrency:
  group: deploy-pr-staging-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  deploy:
    if: github.event.action != 'closed' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
      pull-requests: write
    env:
      STAGING_BRANCH: staging/pr-${{ github.event.pull_request.number }}
      CLOUDFLARE_PAGES_PROJECT: swift-regex-tester
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: vapor/swiftly-action@v0.2
      - run: swiftly install
      - name: Install Swift WebAssembly SDK
        id: setup-sdk
        run: ./setup-swift-sdk.sh

      - name: Build Swift/Wasm bundle
        run: swiftly run swift package --swift-sdk ${{ steps.setup-sdk.outputs.SWIFT_SDK_ID }} js -c release

      - name: Install dependencies
        run: npm ci

      - name: Build site
        run: npm run build

      - name: Publish dist to staging branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: ${{ env.STAGING_BRANCH }}
          force_orphan: true

      - name: Build preview URL
        id: preview
        run: |
          set -euo pipefail
          slug="$(echo "${STAGING_BRANCH}" | tr '[:upper:]' '[:lower:]' | sed -E 's#[^a-z0-9-]+#-#g; s#-+#-#g; s#^-##; s#-$##')"
          echo "url=https://${slug}.${CLOUDFLARE_PAGES_PROJECT}.pages.dev" >> "$GITHUB_OUTPUT"

      - name: Comment preview URL
        uses: actions/github-script@v8
        env:
          PREVIEW_URL: ${{ steps.preview.outputs.url }}
          STAGING_BRANCH: ${{ env.STAGING_BRANCH }}
        with:
          script: |
            const marker = '<!-- pr-staging-preview -->'
            const previewURL = process.env.PREVIEW_URL
            const branch = process.env.STAGING_BRANCH
            const body = `${marker}\n✅ PR staging branch is updated.\n\n- Branch: \`${branch}\`\n- Preview: ${previewURL}`

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100
            })
            const existing = comments.find(comment =>
              comment.user.type === 'Bot' && comment.body?.includes(marker)
            )

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body
              })
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body
              })
            }

  cleanup:
    if: github.event.action == 'closed' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Delete staging branch
        run: |
          set -euo pipefail
          branch="staging/pr-${{ github.event.pull_request.number }}"
          if git ls-remote --exit-code --heads origin "$branch" >/dev/null 2>&1; then
            git push origin --delete "$branch"
            echo "Deleted ${branch}" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "Branch ${branch} does not exist; nothing to delete." >> "$GITHUB_STEP_SUMMARY"
          fi
