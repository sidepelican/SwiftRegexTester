# Swift Regex Tester

A playground for Swift's `Regex` in the browser, powered by WebAssembly.

## Build swift library and generate d.ts

```sh
swiftly run swift package --swift-sdk swift-6.3.2-RELEASE_wasm js
```
